num_pairs = int(input())
test_dict = {}
for _ in range(num_pairs):
    key, *values = input().split()
    test_dict[key] = [int(val) for val in values]
sorted_dict = dict(sorted(test_dict.items(), key=lambda x: sum(x[1])))
for key, values in sorted_dict.items():
    print(f"{key} {sum(values)}")
